query MyTopArtists($limit: Int, $offset: Int) {
    me {
        top_artists(limit: $limit, offset: $offset) {
            limit
            offset
            next
            total
            artists {
                id
            }
        }
    }
}
