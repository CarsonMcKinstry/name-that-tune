query MyTopTracks($limit: Int, $offset: Int) {
    me {
        top_tracks(limit: $limit, offset: $offset) {
            limit
            offset
            next
            total
            tracks {
                id
                is_local
            }
        }
    }
}
