query Track($trackId: ID!) {
    track(id: $trackId) {
        id
        name
        preview_url
        album {
            id
            name
            images {
                url
                height
                width
            }
        }
        artists {
            id
            name
        }
    }
}
